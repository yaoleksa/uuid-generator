# Puropse

This is a CLI tool which generate `UUID` by running uuid command on your terminal

# Short description

This is a pretty simple and useful (at least for me) application. In this repository you can see `app.js` application code file itself and `installer.iss` file as well. In this project I use a simple open sorce installation builder `Inno Setup`[https://jrsoftware.org/isinfo.php]
This is my first real program. "Real program" I mean you can install this by present `.exe` file on your PC